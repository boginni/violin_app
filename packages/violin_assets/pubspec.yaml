name: violin_assets
description: "The assets package for the Violin app"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_localization: ^0.4.0

flutter:
  assets:
    - assets/resources/